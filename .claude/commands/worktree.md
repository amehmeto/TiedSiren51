---
description: Manage git worktrees - create, list, prune merged PRs, or remove.
---

Run the worktree script with the provided arguments:

```bash
./scripts/worktree.sh $ARGUMENTS
```

After the script completes, navigate to the new worktree directory if one was created.
