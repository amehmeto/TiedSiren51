build:
  name: Create a build and run Maestro tests on it
  steps:
    - eas/build
    - eas/maestro_test:
        inputs:
          flow_path: |
            maestro/login_flow.yaml
            maestro/create_blocklist_flow.yaml
            maestro/launch_block_session_flow.yaml
