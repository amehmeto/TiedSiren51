#!/usr/bin/env sh
# Husky v9 stub — delegates to the real hook in .husky/
# core.hooksPath points here (.husky/_), so git runs this stub.
# We forward to the actual implementation in the parent directory.
exec sh "$(dirname "$0")/../pre-push" "$@"
