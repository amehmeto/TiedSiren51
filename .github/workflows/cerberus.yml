# Cerberus Workflow
# Named after the three-headed hound guarding the gates of the underworld.
# Like its mythological counterpart, this workflow acts as a guardian,
# ensuring code quality through multiple checks (tests, builds, lint)
# before allowing passage to the next stage.

name: Cerberus

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Install dependencies
        run: npm ci

      - name: Install rollup platform binary
        run: npm install --no-save @rollup/rollup-linux-x64-gnu

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Lint
        run: npm run lint

      - name: Run unit tests with coverage
        run: npm run test:cov

#      - name: Commit coverage if changed
#        run: bash scripts/commit-coverage.sh

      - name: Store PR coverage
        run: bash scripts/store-coverage.sh pr-coverage.json

      - name: Get base branch coverage
        run: bash scripts/get-base-coverage.sh "${{ github.event.pull_request.base.ref }}"

      - name: Compare coverage and comment
        uses: actions/github-script@v7
        with:
          script: |
            const compareAndComment = require('./scripts/compare-coverage.cjs');
            await compareAndComment({ github, context });

  build:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create google-services.json from secret
        run: |
          printf '%s\n' "$GOOGLE_SERVICES_JSON" > google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: 16.24.1
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci --include=dev

      - name: Install rollup platform binary
        run: npm install --no-save @rollup/rollup-linux-x64-gnu

#      Needs to resolve the issue with a local build inside GitHub Actions and env var (Google Services in particular)
      - name: Local Build
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          npm run build:android:local -- --non-interactive

#      - name: Find APK file
#        id: find_apk
#        run: |
#          APK_PATH=$(find android/app/build/outputs/apk -name "*.apk" -type f | head -n 1)
#          if [ -z "$APK_PATH" ]; then
#            echo "APK not found!"
#            exit 1
#          fi
#          echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
#          echo "apk_name=$(basename $APK_PATH)" >> $GITHUB_OUTPUT
#          echo "Found APK: $APK_PATH"
#
#      - name: Upload APK artifact
#        uses: actions/upload-artifact@v4
#        with:
#          name: app-debug-${{ github.run_number }}
#          path: ${{ steps.find_apk.outputs.apk_path }}
#          retention-days: 7
#
#      - name: Send APK notification to Slack
#        if: success()
#        uses: slackapi/slack-github-action@v1.26.0
#        with:
#          payload: |
#            {
#              "text": "📱 New Android APK ready for QA testing!",
#              "blocks": [
#                {
#                  "type": "header",
#                  "text": {
#                    "type": "plain_text",
#                    "text": "🎉 Android Build Ready for QA"
#                  }
#                },
#                {
#                  "type": "section",
#                  "fields": [
#                    {
#                      "type": "mrkdwn",
#                      "text": "*PR:*\n<${{ github.event.pull_request.html_url }}|#${{ github.event.pull_request.number }} - ${{ github.event.pull_request.title }}>"
#                    },
#                    {
#                      "type": "mrkdwn",
#                      "text": "*Branch:*\n`${{ github.head_ref }}`"
#                    },
#                    {
#                      "type": "mrkdwn",
#                      "text": "*Build:*\n#${{ github.run_number }}"
#                    },
#                    {
#                      "type": "mrkdwn",
#                      "text": "*Author:*\n${{ github.event.pull_request.user.login }}"
#                    }
#                  ]
#                },
#                {
#                  "type": "section",
#                  "text": {
#                    "type": "mrkdwn",
#                    "text": "*APK File:* `${{ steps.find_apk.outputs.apk_name }}`"
#                  }
#                },
#                {
#                  "type": "actions",
#                  "elements": [
#                    {
#                      "type": "button",
#                      "text": {
#                        "type": "plain_text",
#                        "text": "⬇️ Download APK",
#                        "emoji": true
#                      },
#                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
#                      "style": "primary"
#                    },
#                    {
#                      "type": "button",
#                      "text": {
#                        "type": "plain_text",
#                        "text": "📋 View PR",
#                        "emoji": true
#                      },
#                      "url": "${{ github.event.pull_request.html_url }}"
#                    }
#                  ]
#                }
#              ]
#            }
#        env:
#          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#
#      - name: EAS Cloud Preview Build
#        run: eas build -p android --profile preview --non-interactive
#        env:
#          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
