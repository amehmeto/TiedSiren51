# TODO(Node 20 upgrade): Revert to engine-strict=true
# Disabled because oxlint@1.46.0 declares node>=20 but works on Node 18
# via `node node_modules/oxlint/dist/cli.js`. This affects ALL packages,
# so re-enable as soon as the project upgrades to Node 20+.
engine-strict=false
